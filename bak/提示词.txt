1、整体要求@C:\Users\Dean\Documents\Code\project_dianfei目录下建立一个能常州能耗云因驾驶舱，主要是大屏系统，未来会部署在内网中与互联网不同，采用前后端系统，前端h5界面，前端界面自动适应屏幕缩放，后端Python，前端的图表和地图尽量使用采用ECharts控件，数据库使用sqllite数据库，预留mysql数据库，Python调用数据库方式要同时兼容mysql、sqllite。
2、示例明细数据从@data\data.csv 文件，将这个文件的实例数据全部生成一个sqllite数据库表，每一个列都要包含，所有前端界面均需要从该数据表生成。
2、签单界面顶部工具栏依次为：首页、报账管理、电表管理、能耗分析、报表管理，每个界面点击都会切换到相应界面，先是空白的，预留后期完成。
3、首页分3列，第1列包含三行，第1行为能耗总览（支持年、月、日标签切换）。总体能耗、总体电费、POI总数、设备总数；第2行为用电量分项统计（支持年、月、日标签切换）。网络用电、营业用电、办公用电——过程表落地3行为POI分项统计（支持年、月、日标签切换）。网络用电、营业用电、办公用电
POI分项统计（支持年、月、日标签切换）。网络用电、营业用电、办公用电计。
4、第2列为2行，第一行是常州的地图，到各个区县，地图直接使用ECharts调用data\常州市含经开区.json 的地图文件，各个区的名字和轮廓均在地图内，每个名字显示在地图上，当鼠标移到相应的区县，相应的区县变量，整个首页的数据现在该区县的数据。地图可以平移和缩放。第2行水平分两列，第一列用电类型统计（支持年、月、日标签切换）。直供电、转供电、新能源—，第2列能耗趋势（支持年、月、日标签切换），支持月度总能耗趋势图）；
5、第3列分三行，第一行为报账总览（支持年、月、日标签切换）。租费：总报账数、待报账数、已报账数；电费：总报账数、待报账数、已报账数——待报账数据纳管后开发；第二行为告警总览：告警——当前能耗平台；第三行为事件总览：异常调度时间——待异常调度流程纳管后开发。
6、首页的风格可以参考@data\常移能耗运营驾驶舱DEMO.html,仅供参考。

常州能耗云运营驾驶舱开发需求指令
1. 项目基础配置
工作目录：@C:\Users\Dean\Documents\工作\2026\电费系统\project_dianfei

技术架构：前后端分离。

后端：Python (建议 FastAPI 或 Flask)。

前端：H5 响应式大屏，需实现自动等比例缩放适配不同分辨率。

部署环境：内网环境（离线），所有前端资源（ECharts、JS 库等）需确保本地化引用，不依赖互联网。

2. 数据库与数据处理
数据库兼容性：

当前使用 SQLite 数据库。

架构要求：封装数据库操作（如使用 SQLAlchemy ORM），确保代码能同时兼容 MySQL，预留切换开关。

数据初始化：

读取 @data\data.csv 文件。

根据 CSV 列名自动创建 SQLite 表，并导入全量数据。

所有前端图表必须通过后端 API 从该数据表中动态提取。

3. 页面框架与导航
顶部工具栏：首页、报账管理、电表管理、能耗分析、报表管理。

逻辑实现：点击导航切换至对应界面。当前重点开发首页，其余页面暂时预留为空白页。

4. 首页（驾驶舱）详细布局
首页视觉风格请参考 @data\常移能耗运营驾驶舱DEMO.html，整体采用三列式布局。

第一列（左侧统计栏）
能耗总览：支持“年/月/日”标签切换。显示指标：总体能耗、总体电费、POI 总数、设备总数。

用电量分项统计：支持“年/月/日”标签切换。统计类别：网络用电、营业用电、办公用电。

POI 分项统计：支持“年/月/日”标签切换。统计类别：网络用电、营业用电、办公用电。

第二列（中间核心栏）
常州地图（顶部）：

使用 ECharts 调用 @data\常州市含经开区.json 地图文件。

显示各区县名称与轮廓，支持平移与缩放。

交互联动：当鼠标移至/点击特定区县时，首页所有其他组件需同步刷新，展示该区县对应的过滤数据。

下方双列统计（底部）：

左侧：用电类型统计（直供电、转供电、新能源），含年/月/日切换。

右侧：能耗趋势图（支持月度总趋势及年/月/日切换）。

第三列（右侧看板栏）
报账总览：支持“年/月/日”标签切换。区分“租费”与“电费”，分别展示：总报账数、待报账数、已报账数（待报数据预留逻辑接口）。

告警总览：对接当前能耗平台的实时告警数据。

事件总览：展示异常调度事件（预留后续流程纳管接口）。

5. 技术细节要求
图表组件：全量使用 ECharts 控件。

自适应方案：建议使用 CSS transform: scale() 方案或 Flex 布局，确保大屏展示无滚动条、不留黑边。

代码规范：后端逻辑需清晰，数据读取需考虑性能优化


# 项目开发需求书：常州能耗云运营驾驶舱 

## 1. 项目概况
* **目标目录**：`@C:\Users\Dean\Documents\Code\project_dianfei`
* **项目定位**：内网环境部署的政企能耗大屏监控系统，需具备高兼容性与自适应显示能力。
---

## 2. 技术架构要求
* **前端**：
    * 基于 HTML5 的大屏系统。
    * **自适应方案**：必须支持等比例缩放（如 `transform: scale()` 或 `vw/vh`），确保在不同分辨率大屏下无黑边、无滚动条。
    * **离线化**：所有依赖库（ECharts, JS, CSS）需支持本地引用，确保在无互联网的内网环境下正常运行。
* **后端**：
    * 使用 Python (建议 FastAPI 或 Flask) 构建 API 接口。
* **数据库**：
    * **当前环境**：使用 SQLite。
    * **架构设计**：采用 ORM（如 SQLAlchemy）封装，代码逻辑需同时兼容 MySQL，支持通过配置文件无缝切换。

---

## 3. 数据处理逻辑
* **数据初始化**：
    1.  系统启动时，自动读取 `@data\data.csv`。
    2.  将 CSV 映射为 SQLite 表结构，并导入全量实例数据。
* **动态展示**：
    * 首页及各功能模块的所有图表数据，必须通过 API 异步从数据库读取，严禁硬编码静态数据。

---

## 4. 界面布局与功能模块

### 4.1 顶部导航框架
依次包含：**首页**、**报账管理**、**电表管理**、**能耗分析**、**报表管理**。
* 支持路由切换。目前重点实现“首页”，其余界面预留为空白页占位。

### 4.2 首页 (驾驶舱) 三栏布局

#### **左侧栏 (Column 1)**
1.  **能耗总览**：支持 [年/月/日] 切换。展示指标：总体能耗、总体电费、POI总数、设备总数。
2.  **用电量分项统计**：支持 [年/月/日] 切换。展示类别：网络用电、营业用电、办公用电。
3.  **POI分项统计**：支持 [年/月/日] 切换。展示类别：网络用电、营业用电、办公用电。

#### **中间栏 (Column 2)**
1.  **核心地图 (Top)**：
    * 使用 ECharts 加载 `@data\常州市含经开区.json`。
    * 展示常州各区县（含经开区）轮廓与名称。
    * 支持平移与缩放。
    * **数据联动**：鼠标悬停或点击区县时，触发全局过滤器，使首页其他所有图表组件同步刷新为该区县的数据。
2.  **统计图表 (Bottom)**：
    * 左侧：用电类型统计 (直供电、转供电、新能源)。
    * 右侧：能耗趋势图 (支持月度总趋势，具备年/月/日切换逻辑)。

#### **右侧栏 (Column 3)**
1.  **报账总览**：支持 [年/月/日] 切换。细分“租费”与“电费”，展示：总报账数、待报账数、已报账数（待报账数据预留纳管接口）。
2.  **告警总览**：展示当前能耗平台的实时告警列表/状态。
3.  **事件总览**：展示异常调度时间（预留流程管理接口）。

---

## 5. 开发交付物要求
1.  完整的后端 Python 源代码及依赖声明。
2.  前端 H5/CSS/JS 源码（包含本地化的 ECharts 库）。
3.  数据库初始化脚本（CSV 转 DB）。
4.  README 说明文档，包含内网部署步骤。


