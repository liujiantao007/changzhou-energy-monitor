# 常州能耗云运营驾驶舱 - Docker 部署文档

## 部署架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           部署流程架构                                    │
│                                                                          │
│  ┌───────────────────────┐                                              │
│  │  构建服务器（可访问GitHub） │  第一步：在此服务器构建镜像              │
│  │   Ubuntu/Docker 环境    │                                              │
│  └───────────┬───────────┘                                              │
│              │ 镜像文件                                                  │
│              ↓                                                          │
│  ┌───────────────────────┐                                              │
│  │  10.38.78.228:2202     │  第二步：上传镜像到目标服务器                 │
│  │     目标云服务器         │                                              │
│  └───────────┬───────────┘                                              │
│              │ 加载并运行                                                │
│              ↓                                                          │
│  ┌───────────────────────┐                                              │
│  │   Docker 容器运行       │  第三步：运行容器                           │
│  │  - Flask 后端 :5000     │                                              │
│  │  - Nginx 前端 :80       │                                              │
│  │  - 告警同步进程         │                                              │
│  └───────────────────────┘                                              │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 服务器信息

### 目标服务器（10.38.78.228）

| 项目 | 值 |
|------|-----|
| 服务器 IP | 10.38.78.228 |
| SSH 端口 | 2202 |
| 用户名 | root |
| 密码 | Czyd@10086 |

### 构建服务器

需具备以下条件：
- 安装 Docker 和 Docker Compose
- 能够访问 GitHub（克隆代码）
- 能够访问外网（下载 CDN 资源）

---

## 一、完整部署流程

### 第一步：在构建服务器（Ubuntu）构建镜像

```bash
# 1. 克隆代码
git clone https://github.com/liujiantao007/changzhou-energy-monitor.git
cd changzhou-energy-monitor

# 2. 下载 CDN 资源（必须）
mkdir -p js/libs
curl -o js/libs/echarts.min.js https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js
curl -o js/libs/xlsx.full.min.js https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js

# 3. 构建镜像
docker build -t changzhou-energy-monitor:latest .

# 4. 验证镜像已构建
docker images | grep changzhou-energy-monitor
```

### 第二步：导出镜像并上传到目标服务器

```bash
# 在构建服务器上执行

# 1. 导出镜像为 tar 文件
docker save -o changzhou-energy-monitor.tar changzhou-energy-monitor:latest

# 2. 压缩（减小传输体积）
tar -czvf changzhou-energy-monitor.tar.gz changzhou-energy-monitor.tar

# 3. 上传到目标服务器（注意是大写 -P）
scp -P 2202 changzhou-energy-monitor.tar.gz root@10.38.78.228:/root/

# 4. 上传 docker-compose.yml
scp -P 2202 docker-compose.yml root@10.38.78.228:/root/

# 5. 上传 nginx.conf（如果修改过）
scp -P 2202 nginx.conf root@10.38.78.228:/root/
```

### 第三步：在目标服务器加载并运行

```bash
# SSH 连接到目标服务器
ssh -p 2202 root@10.38.78.228
# 输入密码: Czyd@10086

# 1. 解压镜像
gunzip changzhou-energy-monitor.tar.gz

# 2. 加载镜像到 Docker
docker load -i changzhou-energy-monitor.tar

# 3. 确认镜像已加载
docker images | grep changzhou-energy-monitor

# 4. 创建数据目录
mkdir -p data

# 5. 启动容器
docker-compose up -d

# 6. 查看启动日志
docker-compose logs -f
```

### 第四步：验证部署

```bash
# 检查容器状态
docker ps

# 访问服务
curl http://localhost:65080
curl http://localhost:5000/api/health
```

---

## 二、重启与更新流程

代码更新后，需要重新执行完整部署流程：

### 第一步：在构建服务器重新构建

```bash
# 在构建服务器上执行

# 1. 进入项目目录
cd changzhou-energy-monitor

# 2. 拉取最新代码
git pull origin main

# 3. 重新构建镜像
docker build -t changzhou-energy-monitor:latest .

# 4. 导出镜像
docker save -o changzhou-energy-monitor.tar changzhou-energy-monitor:latest
tar -czvf changzhou-energy-monitor.tar.gz changzhou-energy-monitor.tar
```

### 第二步：上传到目标服务器

```bash
# 在构建服务器上执行

# 1. 上传镜像（覆盖旧的）
scp -P 2202 changzhou-energy-monitor.tar.gz root@10.38.78.228:/root/

# 2. 上传 docker-compose.yml（如果更新过）
scp -P 2202 docker-compose.yml root@10.38.78.228:/root/
```

### 第三步：在目标服务器重启

```bash
# SSH 连接到目标服务器
ssh -p 2202 root@10.38.78.228
# 输入密码: Czyd@10086

# 1. 停止旧容器
docker-compose down

# 2. 移除旧镜像（可选，释放空间）
docker rmi changzhou-energy-monitor:latest

# 3. 解压并加载新镜像
gunzip changzhou-energy-monitor.tar.gz
docker load -i changzhou-energy-monitor.tar

# 4. 启动新容器
docker-compose up -d

# 5. 查看日志确认正常
docker-compose logs -f
```

---

## 三、常用运维命令

### 在目标服务器执行

```bash
# 查看容器状态
docker ps

# 查看容器日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 强制重启（重新构建）
docker-compose down && docker-compose up -d
```

---

## 四、容器内进程说明

容器启动后自动运行以下 3 个进程：

| 进程 | 端口 | 说明 |
|------|------|------|
| Flask 后端 | 5000 | 提供 REST API |
| Nginx | 80 | 静态文件服务 + API 反向代理 |
| sync_meter_alarm.py | - | 每 5 分钟同步一次告警数据 |

---

## 五、配置说明

### 环境变量

可在 `docker-compose.yml` 中修改：

```yaml
environment:
  - TZ=Asia/Shanghai
  - DB_HOST=10.38.78.217      # 数据库地址
  - DB_PORT=3220               # 数据库端口
  - DB_USER=liujiantao         # 数据库用户名
  - DB_PASSWORD=Liujt!@#       # 数据库密码
  - DB_NAME=energy_management_2026  # 数据库名
```

### 端口映射

```yaml
ports:
  - "65080:80"    # 前端页面（外部访问 65080）
  - "5000:5000"   # 后端 API
```

---

## 六、架构说明

```
┌─────────────────────────────────────────────┐
│            Docker 容器 (Ubuntu 22.04)       │
│                                              │
│  ┌──────────┐     ┌───────────────────────┐ │
│  │  Nginx   │────→│  静态文件 (index.html │ │
│  │   :80    │     │   js/, css/, data/)   │ │
│  └────┬─────┘     └───────────────────────┘ │
│       │                                        │
│       │ /api/*  反向代理                       │
│       ↓                                        │
│  ┌──────────┐     ┌───────────────────────┐ │
│  │  Flask   │────→│  MySQL (10.38.78.217) │ │
│  │  :5000   │     │  meter_alarm 表       │ │
│  └──────────┘     └───────────────────────┘ │
│                                              │
│  ┌──────────────────┐   ┌─────────────────┐ │
│  │ sync_meter_alarm │──→│ 生产库 (告警同步)│ │
│  │  (每5分钟)        │   │ 10.38.78.228    │ │
│  └──────────────────┘   └─────────────────┘ │
└─────────────────────────────────────────────┘
           │                    │
      65080:80             5000:5000
```

---

## 七、故障排查

### 7.1 页面空白

检查 CDN 资源是否已下载：
```bash
docker exec energy-monitor-prod ls -lh /app/js/libs/
```

### 7.2 数据库连接失败

检查网络和数据库配置：
```bash
docker exec energy-monitor-prod python3 -c "
import pymysql
pymysql.connect(host='10.38.78.217', port=3220, user='liujiantao', password='Liujt!@#', database='energy_management_2026')
print('OK')
"
```

### 7.3 端口被占用

```bash
# Linux
netstat -tlnp | grep -E ':(65080|5000)'
```

### 7.4 查看所有日志

```bash
docker-compose logs -f --tail=100
```

---

## 八、部署完成确认

服务访问地址：

| 服务 | 地址 |
|------|------|
| **前端页面** | http://10.38.78.228:65080 |
| **后端 API** | http://10.38.78.228:5000/api/health |

---

## 九、文件清单

### 构建服务器需要的文件

```
project_dianfeiv2/
├── js/libs/
│   ├── echarts.min.js      # ECharts 库（必须下载）
│   └── xlsx.full.min.js     # Excel 处理库（必须下载）
├── data/
│   └── 常州区县网格地图.json  # 地图数据
├── Dockerfile              # Docker 构建文件
├── docker-compose.yml      # Docker Compose 配置
├── nginx.conf              # Nginx 配置文件
├── entrypoint.sh           # 容器启动脚本
├── requirements.txt        # Python 依赖
└── app.py                  # Flask 后端主程序
```

### 上传到目标服务器的文件

| 文件 | 说明 |
|------|------|
| `changzhou-energy-monitor.tar.gz` | Docker 镜像（压缩包） |
| `docker-compose.yml` | 部署配置文件 |
| `nginx.conf` | Nginx 配置文件（如果修改过） |

---

**文档版本**：v4.0
**最后更新**：2026-05-25
**适用项目**：常州能耗云运营驾驶舱
**目标服务器**：10.38.78.228:2202
