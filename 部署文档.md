# 常州能耗云运营驾驶舱 - Docker 部署文档

## 环境要求

| 项目 | 要求 |
|------|------|
| Docker | 20.10+ |
| Docker Compose | 2.0+ |
| 端口 | 65080, 5000（需开放） |

---

## 一、在线部署（服务器可访问外网）

### 1.1 下载代码

```bash
git clone https://github.com/liujiantao007/changzhou-energy-monitor.git
cd changzhou-energy-monitor
```

### 1.2 首次构建前下载 CDN 资源

```bash
mkdir -p js/libs
curl -o js/libs/echarts.min.js https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js
curl -o js/libs/xlsx.full.min.js https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js
```

### 1.3 构建并启动

```bash
# 构建镜像
docker build -t changzhou-energy-monitor:latest .

# 启动容器（后台运行）
docker-compose up -d

# 查看启动日志
docker-compose logs -f
```

### 1.4 验证服务

```bash
# 检查容器状态
docker ps

# 访问前端页面
curl http://localhost:65080

# 访问后端健康检查
curl http://localhost:5000/api/health
```

---

## 二、离线部署（内网环境）

### 2.1 在有外网环境构建并导出

```bash
# 1. 克隆代码
git clone https://github.com/liujiantao007/changzhou-energy-monitor.git
cd changzhou-energy-monitor

# 2. 下载 CDN 资源（必须）
mkdir -p js/libs
curl -o js/libs/echarts.min.js https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js
curl -o js/libs/xlsx.full.min.js https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js

# 3. 构建镜像
docker build -t changzhou-energy-monitor:latest .

# 4. 导出镜像为 tar 文件
docker save -o changzhou-energy-monitor.tar changzhou-energy-monitor:latest

# 5. 压缩（减小传输体积）
tar -czvf changzhou-energy-monitor.tar.gz changzhou-energy-monitor.tar
```

### 2.2 传输到内网服务器

使用 scp、U 盘或其他方式将以下文件复制到内网服务器：

| 文件 | 说明 |
|------|------|
| `changzhou-energy-monitor.tar.gz` | Docker 镜像（压缩包） |
| `docker-compose.yml` | 部署配置文件 |

### 2.3 在内网服务器加载并启动

```bash
# 1. 解压镜像
gunzip changzhou-energy-monitor.tar.gz

# 2. 加载镜像到 Docker
docker load -i changzhou-energy-monitor.tar

# 3. 确认镜像已加载
docker images | grep changzhou-energy-monitor

# 4. 创建数据目录
mkdir -p data

# 5. 启动容器
docker-compose up -d

# 6. 查看启动日志
docker-compose logs -f
```

### 2.4 验证服务

```bash
# 检查容器状态
docker ps

# 访问前端页面
curl http://localhost:65080

# 访问后端健康检查
curl http://localhost:5000/api/health
```

---

## 三、部署到内网服务器（10.38.78.228）

### 3.1 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | 10.38.78.228 |
| SSH 端口 | 2202 |
| 用户名 | root |
| 密码 | Czyd@10086 |

### 3.2 方式一：本地导出后上传（适用于首次部署或镜像较大）

**第一步：在本地执行**

```bash
# 在本项目目录下执行导出
docker save -o changzhou-energy-monitor.tar changzhou-energy-monitor:latest
tar -czvf changzhou-energy-monitor.tar.gz changzhou-energy-monitor.tar

# 传输镜像到服务器（注意是大写 -P）
scp -P 2202 changzhou-energy-monitor.tar.gz root@10.38.78.228:/root/

# 传输 docker-compose.yml
scp -P 2202 docker-compose.yml root@10.38.78.228:/root/
```

**第二步：在服务器执行**

```bash
# SSH 连接到服务器
ssh -p 2202 root@10.38.78.228
# 输入密码: Czyd@10086

# 解压并加载镜像
gunzip changzhou-energy-monitor.tar.gz
docker load -i changzhou-energy-monitor.tar

# 确认镜像已加载
docker images | grep changzhou-energy-monitor

# 启动服务
mkdir -p data
docker-compose up -d

# 查看日志
docker-compose logs -f
```

### 3.3 方式二：服务器直接构建（适用于网络条件允许）

```bash
# SSH 连接到服务器
ssh -p 2202 root@10.38.78.228
# 输入密码: Czyd@10086

# 克隆代码
git clone https://github.com/liujiantao007/changzhou-energy-monitor.git
cd changzhou-energy-monitor

# 下载 CDN 资源
mkdir -p js/libs
curl -o js/libs/echarts.min.js https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js
curl -o js/libs/xlsx.full.min.js https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js

# 构建并启动
docker build -t changzhou-energy-monitor:latest .
docker-compose up -d
```

### 3.4 验证部署

```bash
# 检查容器状态
docker ps

# 访问服务
curl http://localhost:65080
curl http://localhost:5000/api/health
```

### 3.5 部署完成

服务访问地址：

| 服务 | 地址 |
|------|------|
| **前端页面** | http://10.38.78.228:65080 |
| **后端 API** | http://10.38.78.228:5000/api/health |

---

## 四、运维管理

### 4.1 常用命令

```bash
# 查看容器状态
docker ps

# 查看容器日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 重新构建并启动
docker-compose up -d --build
```

### 4.2 更新部署

```bash
# 在线环境
git pull origin main
docker-compose up -d --build
```

### 4.3 容器内进程

容器启动后自动运行以下 3 个进程：

| 进程 | 端口 | 说明 |
|------|------|------|
| Flask 后端 | 5000 | 提供 REST API |
| Nginx | 80 | 静态文件服务 + API 反向代理 |
| sync_meter_alarm.py | - | 每 5 分钟同步一次告警数据 |

---

## 五、配置说明

### 5.1 环境变量

可在 `docker-compose.yml` 中修改：

```yaml
environment:
  - TZ=Asia/Shanghai
  - DB_HOST=10.38.78.217      # 数据库地址
  - DB_PORT=3220               # 数据库端口
  - DB_USER=liujiantao         # 数据库用户名
  - DB_PASSWORD=Liujt!@#       # 数据库密码
  - DB_NAME=energy_management_2026  # 数据库名
```

### 5.2 端口映射

```yaml
ports:
  - "65080:80"    # 前端页面（外部访问 65080）
  - "5000:5000"   # 后端 API
```

### 5.3 数据同步

- **告警同步**：每 5 分钟自动从 `10.38.78.228:53306`（生产库）同步告警数据到本地 `meter_alarm` 表
- **同步日志**：查看容器日志可以看到同步状态

---

## 六、架构说明

```
┌─────────────────────────────────────────────┐
│            Docker 容器 (Ubuntu 22.04)       │
│                                              │
│  ┌──────────┐     ┌───────────────────────┐ │
│  │  Nginx   │────→│  静态文件 (index.html │ │
│  │   :80    │     │   js/, css/, data/)   │ │
│  └────┬─────┘     └───────────────────────┘ │
│       │                                        │
│       │ /api/*  反向代理                       │
│       ↓                                        │
│  ┌──────────┐     ┌───────────────────────┐ │
│  │  Flask   │────→│  MySQL (10.38.78.217) │ │
│  │  :5000   │     │  meter_alarm 表       │ │
│  └──────────┘     └───────────────────────┘ │
│                                              │
│  ┌──────────────────┐   ┌─────────────────┐ │
│  │ sync_meter_alarm │──→│ 生产库 (告警同步)│ │
│  │  (每5分钟)        │   │ 10.38.78.228    │ │
│  └──────────────────┘   └─────────────────┘ │
└─────────────────────────────────────────────┘
           │                    │
      65080:80             5000:5000
```

---

## 七、故障排查

### 7.1 页面空白

检查 CDN 资源是否已下载：
```bash
docker exec energy-monitor-prod ls -lh /app/js/libs/
```

### 7.2 数据库连接失败

检查网络和数据库配置：
```bash
# 测试数据库连接
docker exec energy-monitor-prod python3 -c "
import pymysql
pymysql.connect(host='10.38.78.217', port=3220, user='liujiantao', password='Liujt!@#', database='energy_management_2026')
print('OK')
"
```

### 7.3 端口被占用

```bash
# Windows
netstat -ano | Select-String ":65080|:5000"

# Linux
netstat -tlnp | grep -E ':(65080|5000)'
```

### 7.4 查看所有日志

```bash
docker-compose logs -f --tail=100
```

---

## 八、文件清单

部署时需要包含以下文件：

```
project_dianfeiv2/
├── js/libs/
│   ├── echarts.min.js      # ECharts 库（必须下载）
│   └── xlsx.full.min.js     # Excel 处理库（必须下载）
├── data/
│   └── 常州区县网格地图.json  # 地图数据
├── Dockerfile              # Docker 构建文件
├── docker-compose.yml      # Docker Compose 配置
├── nginx.conf              # Nginx 配置文件
├── entrypoint.sh           # 容器启动脚本
├── requirements.txt         # Python 依赖
└── app.py                   # Flask 后端主程序
```

---

**文档版本**：v3.0
**最后更新**：2026-05-25
**适用项目**：常州能耗云运营驾驶舱
**部署服务器**：10.38.78.228:2202
